[center][size=6][b]MW3 (2011) Native Controller Support[/b][/size]
[size=3]Campaign & Survival[/size][/center]

[center][color=#E74C3C][b]⚠ THIS IS EXPERIMENTAL, ALPHA-STAGE SOFTWARE ⚠[/b][/color][/center]

It hooks directly into a live game process. Expect bugs, rough edges, unfinished features, and the occasional need to fall back to keyboard/mouse. Use at your own risk. Not affiliated with, endorsed by, or sponsored by Activision, Infinity Ward, or any of their affiliates.

[hr]

[color=#E74C3C][b]⚠ Only v0.2.2 and v0.3.2+ are currently distributed here:[/b][/color] versions before v0.2.2 shipped with an aim-assist feature's code compiled into the DLL (disabled by default, but present in the binary) and were removed as a VAC-risk precaution. Everything between v0.2.2 and the current release (v0.2.5 through v0.3.1.h1) was rapid, in-progress alpha churn and has also been unpublished — not a security concern, just not worth keeping downloadable alongside the current release. Nothing has been deleted — full source history for every version remains available in the GitHub repository — only the downloadable packages are no longer offered. See the Changelog tab for full detail.

[color=#F39C12][b]A note on antivirus scans:[/b][/color] a small number of scanners have flagged released DLLs with generic heuristic labels — v0.3.0 got 1/67 on VirusTotal (VBA32, generic label [font=Courier New]dbadur[/font]); v0.3.1 got a report of MaxSecure flagging it as [font=Courier New]Trojan.Malware.300983.susgen[/font]. Every major vendor (Microsoft, Kaspersky, ESET, Malwarebytes, CrowdStrike, BitDefender, and more) has shown clean on every release so far. Both labels are generic "suspicious-generic" heuristic buckets, not a named-family match — consistent with a heuristic false positive on the DLL's real, intentional behavior — hooking functions inside the game process via MinHook and runtime byte-pattern scanning — the same general shape plenty of legitimate tools (ReShade, RivaTuner Statistics Server) trigger on some scanners. Full source is on GitHub if you want to verify yourself.

[hr]

[size=4][b]Status: ALPHA — v0.3.3 (2026-08-18) — real, extended-session performance stuttering fixed[/b][/size]

[color=#E74C3C][b]Headline fix:[/b][/color] real, extended-session performance stuttering, three separate evidence-backed causes found and fixed — a log file that was never trimmed between launches, a controller-poll thread that never stopped rescanning every device, and a diagnostic log whose dedup logic didn't actually dedup. A/B tested directly against v0.2.2 to confirm the regression was real, not imagined. Also fixes vibration getting stuck on across a pause/menu, and a real co-op bug where damage-rumble could trigger off a teammate's hits instead of your own. Ships a first, [b]PREVIEW/WIP[/b] pass at DualSense Bluetooth support — [b]stick input does not work correctly over Bluetooth, confirmed on real hardware[/b]; USB DualSense support (v0.3.2) still hasn't been independently confirmed working either. Full detail in the Changelog tab and GitHub [font=Courier New]re_notes/known_issues.md[/font] issue #79. Not feature-complete, not fully tested end-to-end. Multiplayer ([font=Courier New]iw5mp.exe[/font]) hasn't been started at all. Read [b]What Works / Partial / Not Working[/b] below before assuming any specific feature works — this project is deliberately honest about what's confirmed vs. not, not a marketing pitch.

[hr]

[size=4][b]What This Is[/b][/size]

MW3 (2011) shipped on PC with [b]zero working controller input path[/b] — no [font=Courier New]xinput[/font]/[font=Courier New]dinput8[/font] import anywhere in the binary, no hidden setting to unlock. This is a from-scratch native controller project, not a keyboard/mouse-emulation mapper: analog movement, look, and most buttons are driven directly through the game's own real internal engine calls — the exact same [font=Courier New]usercmd_t[/font] fields, [font=Courier New]kbutton_t[/font] states, and view-angle accumulators a real keyboard/mouse player already uses, just fed from a controller instead.

Every other "controller support" option for this game works by faking keyboard/mouse input underneath a mapper tool — poll → convert to a key/mouse event → OS input queue → the game's own input processing. That's a real, measurable extra layer between the stick and the game. This project writes straight into the engine's real per-frame input path from inside the game's own process, on the game's own frame tick — no OS-level input event, no intermediate queue. That's the core advantage: input feel that matches native console analog input, not an approximation of it.

[b]Three narrow, deliberate exceptions[/b] to that rule exist (documented, not hidden) for inputs where an extensive search found no locatable native trigger: Survival's ready-up prompt, D-pad Left's AI-squadmate call-in, and the Back button's scoreboard/objectives — all three synthesize a real keypress via [font=Courier New]PostMessage[/font]/[font=Courier New]SendInput[/font] rather than driving the engine directly. Everything else — including all of movement, look, and combat — drives real engine state.

[hr]

[size=4][b]✅ What Works (confirmed live, Campaign/Survival)[/b][/size]

[list]
[*]Analog movement (left stick) and analog look (right stick), with independently configurable horizontal/vertical sensitivity, separate from the mouse pipeline
[*]Console-accurate look acceleration ramp (33ms — tied to this old engine's own locked 30fps tick, not a guessed value)
[*]Fire, Aim Down Sights (true hold, not a toggle), Melee, Tactical/Lethal, Jump
[*]ADS zoom-aware look slowdown, so magnified scopes don't feel absurdly twitchy, plus a separately-tunable close-range slowdown for pistols/iron sights
[*]Crouch/Prone 3-state stance ladder — tap vs. hold, matching console. Two intermittent-failure bugs fixed and user-confirmed live in v0.2.5
[*]Interact vs. Reload on the same button (hold vs. tap, like console)
[*]Weapon switch, D-pad killstreak/attachment actionslots
[*][b]Sprint[/b] — drives the real native [font=Courier New]+sprint[/font] kbutton; the engine's own stamina/recovery timer and the Extreme Conditioning perk's duration override both apply automatically, no custom timer code needed
[*][b]Hold Breath[/b] (L3 while ADS'd on a sniper) — steadies aim while held, accuracy drops once breath runs out; genuinely native input. A critical regression (couldn't fire while breath was held) was fixed and user-confirmed live in v0.2.5
[*]Pause menu open [i]and[/i] close via real engine calls
[*]B backs out of any open menu, on top of its normal crouch/prone role
[*]Full D-pad + A menu navigation — main menu, title screen, pause menu, options screens (including slider value adjustment), buy-station/armory lists
[*]Survival's between-wave ready-up
[*]3 of Survival's 4 real killstreaks: Predator Missile launch, Precision Airstrike, AI squadmate call-in
[*]Button/stick layout presets (Default/Tactical/Lefty/TacticalLefty) — [b]all confirmed correct against real hardware[/b]
[*]Fully configurable via a self-documenting [font=Courier New]mw3ncp_config.ini[/font] (sensitivity, ADS slowdown, hold thresholds, bindings, look ramp, notification style). [b]New in v0.2.5:[/b] the config now auto-migrates existing files across updates, and [b]hot-reloads live[/b] while the game is running — no relaunch needed to see most changes
[*][b]New in v0.2.5:[/b] a small on-screen top-right notification on startup and whenever the config is hot-reloaded, with a rare chance of a stylized variant. Uses a self-contained, DLL-embedded font — no font install required
[*][b]New in v0.3.0, confirmed live:[/b] real controller-glyph icons — in-game interact hints (pickup/swap, buy-station, mantle, Reload, grenade throwback, Survival ready-up) and menu corner hints (Back/Friends). Also new: a custom mouse cursor overlay so the cursor draws correctly on top of these icons instead of underneath them
[*][b]New in v0.3.0:[/b] real controller vibration on weapon fire and taking damage — went through four tuning rounds and is now maxed out in software; physically confirmed working
[*][b]New in v0.3.0:[/b] crouch input occasionally not responding, Survival ready-up UI conflicts, mistargeted menu text, Jump not standing you up first, and cursor visibility issues — a full bug batch from the first public Survival co-op stream, all fixed and confirmed live
[*][b]New in v0.3.1, confirmed live:[/b] controller-glyph icons now show correctly in every game language — root cause was several detection sites matching the game's own on-screen LOCALIZED text against a hardcoded English word; fixed by wiring in the game's own real localized-string resolver instead
[*][b]New in v0.3.2, confirmed live:[/b] controller-glyph icons ACTUALLY draw now, for everyone — the v0.3.0-era claims above were true only on the developer's own machine. This project's own master enable flag for the whole glyph overlay was hardcoded off in every shipped release, undocumented, and never exposed in any ini or in-game toggle — root-caused via direct reproduction and fixed; see the Changelog tab and GitHub [font=Courier New]known_issues.md[/font] issue #74 for the full story
[/list]

[size=4][b]🟡 Partial — works, with a specific known gap[/b][/size]

[list]
[*]Predator Missile's post-fire guidance — launch works, steering the flying missile doesn't yet
[*]Back button (scoreboard/objectives) — implemented and builds clean, not yet separately confirmed live
[*]A few Campaign killstreak/vehicle set-pieces (DPV aiming, a mounted turret sequence, one mortar sequence) — aim or fire input has a specific, documented gap; most of Campaign plays fine
[*]Highlighted-item glyph in menu list navigation — shown only on a handful of individually-verified screens (main menu, Campaign hub, a couple of popups) rather than everywhere yet, as a deliberate release-safety choice after an audit found several "done" screens were never actually re-confirmed
[*]Vibration doesn't register hits absorbed by Survival's purchasable Body Armor — a separate value from real health this project hasn't located in memory yet
[*]A few menu glyph icons can land in a slightly wrong POSITION under non-English languages — icon selection is correct everywhere now (fixed in v0.3.1), only fine positioning on a handful of screens isn't yet
[/list]

[size=4][b]🧪 Preview / WIP — real, working code, but not yet played[/b][/size]

[list]
[*][b]New in v0.3.1:[/b] a from-scratch replacement Options screen covering every real vanilla setting (Look/Video/Audio/Voice/Advanced Video/Movement/Actions) plus real keybind rebind capture and a custom controller-bindings drill-down. The original plan (inject content into the real menu system) hit a genuine engine limit and was abandoned in favor of a fully custom-drawn screen instead. Compiles clean and follows every pattern already live-confirmed elsewhere in this project — but genuinely has not been played. [b]Off by default, no in-game toggle[/b] — enable at your own risk via [font=Courier New][Options] UseCustomOptionsScreen=1[/font] in [font=Courier New]mw3ncp_config.ini[/font]. See the GitHub repo's [font=Courier New]re_notes/known_issues.md[/font] issue #66 for the full, itemized status before trying it.
[*][b]New in v0.3.2:[/b] native DualSense (PS5 controller) support via a raw-HID backend, bypassing Steam Input entirely, plus a first pass at native gyro-aim riding the same connection. Sticks/buttons/triggers work exactly like an XInput pad to the rest of this project; implemented against a real, working reference implementation's confirmed byte offsets, not the developer's own hardware (none is available). [b]Not yet live-tested against real hardware[/b] — gyro units are raw/uncalibrated, axis mapping is a best-effort guess with invert toggles to correct it if wrong, USB only (no Bluetooth yet). Off by default — enable via [font=Courier New][Gyro] Enabled=1[/font] in [font=Courier New]mw3ncp_config.ini[/font]. See [font=Courier New]known_issues.md[/font] issue #76.
[/list]

[size=4][b]⬜ Not Working / Not Implemented At All[/b][/size]

[list]
[*][b]Auto-mantle while sprinting — implemented, live-tested twice, still doesn't reliably fire near a real ledge.[/b] Shipped OFF by default. Two rounds attempted (a jump-spam regression fixed, then a render/gameplay-tick timing gap fixed) but neither confirmed the feature actually firing — treat as not implemented, not as a working opt-in
[*]Per-animation-step reload vibration (a pulse per real reload animation beat, matching classic console CoD) — a committed future feature, not yet built
[*]One vehicle-exit prompt (a specific tank-exit sequence) — not wired up
[*]Survival debug menu / dev console — the real in-game console is confirmed permanently dead
[*][b]Multiplayer ([font=Courier New]iw5mp.exe[/font])[/b] — not started at all, separate binary, needs its own full reverse-engineering pass
[/list]

[hr]

[size=4][b]Installation[/b][/size]

[list=1]
[*]Copy [font=Courier New]d3d9.dll[/font] from this download into your MW3 install folder — the same folder as [font=Courier New]iw5sp.exe[/font].
[*]Launch the game and start a Campaign or Survival session as normal.
[*]A [font=Courier New]proxy_d3d9.log[/font] file will appear in that folder once the DLL is running — the first thing to check if something goes wrong, and the most useful thing to attach to a bug report.
[/list]

[b]Uninstalling:[/b] just delete [font=Courier New]d3d9.dll[/font]. Nothing else is touched — the base game files are never modified.

[color=#F39C12][b]Note:[/b][/color] Steam's "Verify integrity of game files" (or an automatic game update) may silently remove a dropped-in [font=Courier New]d3d9.dll[/font]. If controller input stops working after an update, that's the likely reason — just reinstall.

[hr]

[size=4][color=#E74C3C][b]⚠ Do NOT use this with Plutonium multiplayer[/b][/color][/size]

Plutonium's own anti-cheat is confirmed to ban DLL injection and memory access — a 7-day ban on first offense, permanent after. This project's entire architecture (a proxy [font=Courier New]d3d9.dll[/font], function hooking) is exactly what that system is built to catch, regardless of this being input-only rather than a gameplay cheat. This is a real, confirmed risk, not theoretical. Supported right now: [b]retail Steam Campaign/Survival only[/b].

[hr]

[size=4][b]Credits & License[/b][/size]

This project bundles and links [url=https://github.com/TsudaKageyu/minhook]MinHook[/url] (Copyright © 2009–2017 Tsuda Kageyu, BSD 2-Clause-style license) for all API hooking, and the Hacker Disassembler Engine (HDE) 32/64 C it bundles.

Released under a custom, permissive license: free to use, modify, and fork. The one restriction — neither this project nor any fork/derivative may ever be sold or charged for; it must stay free for everyone. Because of that restriction this doesn't meet the OSI's formal "open source" definition, but the source is fully open. Does not grant any rights to Call of Duty: Modern Warfare 3 itself — you need your own legitimate copy of the game.

[hr]

[size=4][b]Links[/b][/size]

Full source, reverse-engineering write-ups, and the complete patch history live on GitHub — see this page's Source/Files links. Bug reports and contributions welcome there.
