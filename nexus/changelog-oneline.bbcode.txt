[b]v0.3.3[/b] (2026-08-18) — Real, extended-session performance stuttering fixed: three separate causes found and fixed (a log file never trimmed between launches, a controller-poll thread that never stopped rescanning every device, a diagnostic log whose dedup didn't dedup), A/B tested directly against v0.2.2 to confirm the regression was real. Also fixes vibration getting stuck on across a pause/menu and a co-op bug where damage-rumble could trigger off a teammate's hits. Ships a first, PREVIEW/WIP pass at DualSense Bluetooth support — stick input does not work correctly over Bluetooth, confirmed on real hardware.
[b]v0.3.2[/b] (2026-08-16) — Controller-glyph icons actually work now: root-caused to this mod's OWN master enable flag shipping hardcoded off since v0.3.0 (16 days, two investigation rounds to find — full postmortem on GitHub), now fixed and confirmed live, flag removed entirely so upgraders can't get stuck on the old value. Also: a first, PREVIEW/WIP pass at native DualSense (PS5) support + gyro-aim, off by default, not yet hardware-tested.
[b]v0.3.1.h1[/b] (2026-08-09) — Feature-free hotfix: a critical mouse-lag regression fixed (confirmed live), controller-glyph icons never appearing on non-16:9 resolutions fixed (two separate confirmed bug classes), a real Options-screen crash-risk fix, and a log-volume fix for long sessions.
[b]v0.3.1[/b] (2026-08-06) — Stabilization release (same treatment as v0.2.2, no follow-up planned for ~2 weeks barring a critical issue). Headline fix: controller-glyph icons were silently broken for every non-English game language — root-caused against the game's own real localization resolver and confirmed live, not worked around. Also ships a from-scratch replacement Options screen (every real vanilla setting, real keybind rebind capture) as an explicitly labeled preview/WIP feature — off by default, ini-only, not yet played.
[b]v0.3.0[/b] (2026-08-03) — Most significant release to date: real controller-glyph icons (in-game hints, menu corner hints, custom cursor overlay), real controller vibration (physically confirmed, maxed out in software), and a full crouch/UI bug batch from the first public Survival co-op stream, all confirmed live. Auto-mantle while sprinting and Body Armor damage vibration are honestly flagged as NOT working, shipped disabled/incomplete rather than glossed over.
[b]v0.2.5[/b] (2026-07-31) — Hotfix: crouch/stance reliability fully fixed (two rounds), a critical Hold Breath/Fire fix, split look sensitivity, config auto-migration + live hot-reload, and a new on-screen notification feature with a self-contained embedded font.
[b]v0.2.2[/b] (2026-07-20) — Risk-mitigation release: aim assist permanently removed (not just disabled) following anti-cheat risk research; updating from v0.2.1 or earlier is recommended.
[b]v0.2.1[/b] (2026-07-20) — Added a console-accurate look acceleration ramp; Hold Breath (L3 while ADS'd) is now fully working as genuine native input.
[b]v0.2.0[/b] (2026-07-19) — Sprint now drives its real native kbutton (no custom timer needed); 3 of 4 Survival killstreaks confirmed working; menu navigation extended to the main menu/title screen.
[b]v0.1.3[/b] (2026-07-17) — Real D-pad/A menu navigation confirmed working live (main menu, pause menu, options screens).
[b]v0.1.2[/b] (2026-07-16) — Documentation-accuracy pass; one small INI comment fix, no behavior change.
[b]v0.1.1[/b] (2026-07-16) — Fixed a regression that broke vanilla keyboard sprint, and a bug that could invert look direction on deep-zoom optics.
[b]v0.1.0-prealpha[/b] (2026-07-15) — Initial release: analog movement/look, core combat buttons, stance ladder, sprint, pause menu, D-pad, and Survival ready-up.
