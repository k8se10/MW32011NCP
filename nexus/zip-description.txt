Main file (v0.3.1.h1 — feature-free hotfix on v0.3.1. Fixes a critical mouse-lag regression, confirmed live; controller-glyph icons never appearing on non-16:9 resolutions, root-caused to two separate confirmed bug classes; a real Options-screen crash risk; and unbounded log growth on long sessions). Contains d3d9.dll, LICENSE, PATCHNOTES.md, and README.txt. Extract d3d9.dll into your MW3 install folder (same folder as iw5sp.exe) — see README.txt inside the archive for full install/uninstall steps and the current what-works breakdown.
