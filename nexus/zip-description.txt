Main file (v0.3.2 — controller-glyph icons actually work now. This project's own master enable flag for the glyph overlay shipped hardcoded off since v0.3.0 and was never flipped on for release — root-caused via direct reproduction and fixed, confirmed live, flag removed entirely so upgraders can't get stuck on the old broken value; see the Changelog tab. Also new, PREVIEW/WIP off by default: native DualSense controller support + gyro-aim, not yet hardware-tested). Contains d3d9.dll, LICENSE, PATCHNOTES.md, and README.txt. Extract d3d9.dll into your MW3 install folder (same folder as iw5sp.exe) — see README.txt inside the archive for full install/uninstall steps and the current what-works breakdown.
