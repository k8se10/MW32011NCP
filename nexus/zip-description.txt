🚨 BROKEN as of 2026-09-03: MW3 (2011) was recompiled to 64-bit and this 32-bit release can no longer load at all. All support for this and every other -x86 release is discontinued, effective immediately. Do not download expecting it to work — see known_issues.md issue #111 on GitHub. 🚨

Main file (v0.3.5-x86 — largest release yet by commit volume: a new visual-enhancement suite (InternalRenderScalePercent with real, confirmed GPU cost scaling; FSR 1.0 RCAS sharpening; camera-only motion blur; forced anisotropic filtering — all off by default) and a full stutter/threading-architecture overhaul (a recurring 2-5 second freeze root-caused to five causes plus a self-inflicted regression from the poll-thread fix itself, fixed via four dedicated background threads). ForceD3D9On12 and the in-mod FPS limiter are both removed entirely after real-world testing found them unsafe/non-functional. Also ships ForceHighQualityShadows/ForceHighQualityLighting and a [Gyro] config-generation fix. Investigated, not shipped: Campaign scripted sequences (QTEs) still ignore controller input entirely; see the Changelog tab). Contains d3d9.dll, LICENSE, PATCHNOTES.md, and README.txt. Extract d3d9.dll into your MW3 install folder (same folder as iw5sp.exe) — see README.txt inside the archive for full install/uninstall steps and the current what-works breakdown.
