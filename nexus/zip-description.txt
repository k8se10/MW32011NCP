Main file (v0.3.1 — stabilization release, same treatment as v0.2.2: no follow-up planned for ~2 weeks barring a critical issue. Fixes controller-glyph icons never appearing for non-English game languages, confirmed live; also ships a from-scratch replacement Options screen as an explicitly labeled preview/WIP feature, off by default). Contains d3d9.dll, LICENSE, PATCHNOTES.md, and README.txt. Extract d3d9.dll into your MW3 install folder (same folder as iw5sp.exe) — see README.txt inside the archive for full install/uninstall steps and the current what-works breakdown.
