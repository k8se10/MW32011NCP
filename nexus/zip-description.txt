Main file (v0.3.4 — DualSense's Bluetooth stick-garbling bug is fixed, confirmed live by the reporting tester: a real signed-16-bit integer overflow at full-forward stick deflection. The fix lives in code shared with the USB path, so it almost certainly applies there too, but USB DualSense input still has not been independently confirmed working by a separate tester. Also: the in-game glyph position editor (F2/F3) extended from menu items to real gameplay hints, auto-mantle while sprinting confirmed working (still off by default), a new opt-in plugin API, and a glyph-icon fringing fix. Controller-glyph coverage for QTE prompts was investigated but deliberately parked, not shipped this release; see the Changelog tab). Contains d3d9.dll, LICENSE, PATCHNOTES.md, and README.txt. Extract d3d9.dll into your MW3 install folder (same folder as iw5sp.exe) — see README.txt inside the archive for full install/uninstall steps and the current what-works breakdown.
