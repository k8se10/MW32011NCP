Main file (v0.3.3 — real, extended-session performance stuttering fixed. Three separate causes found and fixed via direct evidence: a log file that was never trimmed between launches, a controller-poll thread that never stopped rescanning every device, and a diagnostic log whose dedup logic didn't actually dedup. A/B tested directly against v0.2.2 to confirm the regression was real. Also fixes vibration getting stuck on across a pause/menu and a real co-op bug where damage-rumble could trigger off a teammate's hits. Ships a first PREVIEW/WIP pass at DualSense Bluetooth support — stick input does not work correctly over Bluetooth, confirmed on real hardware; see the Changelog tab). Contains d3d9.dll, LICENSE, PATCHNOTES.md, and README.txt. Extract d3d9.dll into your MW3 install folder (same folder as iw5sp.exe) — see README.txt inside the archive for full install/uninstall steps and the current what-works breakdown.
